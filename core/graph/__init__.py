"""LangGraph workflow: explicit state, nodes, and transitions.

Research state is stored and passed explicitly rather than accumulating inside
agent memory, so any run can be inspected, checkpointed, and resumed.
"""
